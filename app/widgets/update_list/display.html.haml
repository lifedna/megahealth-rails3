= widget_div do
  = render({:state => :list}, @activities)

- unless @activities.last_page?  
  = render_widget :load_more_stream, :display, :page_num => @activities.current_page + 1
  #spinner.text-center
    %span.icon.spin.icon-32.icon-spinner
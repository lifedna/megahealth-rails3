= widget_div do
  = form_for @section, :url => url_for_event(:submit, :id => @section.id), :remote => true do |f|
    / = f.error_messages
    = f.text_field :name, :size => 20
    %p
    = f.submit "更名"  

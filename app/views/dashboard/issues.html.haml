- @page = "dashboard"

/ Page
.site-center.center-page
  = render :partial => "dashboard/top_menu", :locals => {:selected => "issues"}

  .col.size1of4
    .list{:style => "border-bottom: 1px solid #F1F1F1;padding: 0 10px 20px 0;"} 
      %h4 我的健康问题
      .menu
        %ul.stat.left.nav
          - @phis.each do |i|
            %li{:class => "#{i.name == params[:name]? 'active' : ''}"}
              %a{:href => "#{issues_path(:name => i.name)}"} 
                / %span.data #{i.stories.count}
                #{i.name}

    .margin-t20
      = link_to '增加', '#'

  .col.size3of4
    .section-content.margin-l20
      / - if params[:name]
      /   %p 
      /     你有
      /     %b #{@stories.count}
      /     篇关于
      /     %b #{@phi.name}
      /     的经历分享
      / - else 
      /   %p 
      /     你有
      /     %b #{@stories.count}
      /     篇经历分享
      - if params[:name]
        %h3.background-green{:style => "padding: 10px;line-height: 1.3;"} 
          #{@phi.name}
          .label.background-orange
            = link_to '我来分享', new_issue_story_path(@phi.issue)
      .tab-block
        .top-nav           
          .tabs        
            %ul.nav
              %li.active
                = link_to '大家的分享', '#tabcontent1'
              %li
                = link_to '我的分享', '#tabcontent2'
              %li 
                = link_to '我回应的分享', '#tabcontent3'
                          
          .tab-content
            #tabcontent1.cell
              %table.horizontal-border
                %tbody
                  - if @phi.issue.stories.empty?
                    %p
                      %b 目前还没有经历分享！
                    %p.color-pink
                      健康的决定是很难的，并寻找合适的医疗保健提供者显然是费劲。医疗治病经验应该是透明的，它可以（而且应该）通过与其他有相同境地的人分享被加强了。讲述您的经历，让其他人可以向他们学习，是更明智的决策。
                    = link_to '我来分享', new_issue_story_path(@phi.issue), :class => "button background-orange" 

                  - @phi.issue.stories.each do |s|
                    %tr 
                      %td #{link_to s.title, issue_story_path(s.issue, s)}
                      %td #{s.comments_count}回应
                      %td 一天前
                      / %td #{s.last_updated_at.strftime("%Y-%m-%d %H:%M:%S")}
                      %td #{s.user.name}
            #tabcontent2.cell.hidden-tab
              %table.horizontal-border
                %tbody
                  - if @phi.stories.empty?
                    %p
                      %b 我还没有任何分享！
                    %p.color-pink
                      健康的决定是很难的，并寻找合适的医疗保健提供者显然是费劲。医疗治病经验应该是透明的，它可以（而且应该）通过与其他有相同境地的人分享被加强了。讲述您的经历，让其他人可以向他们学习，是更明智的决策。
                    = link_to '我来分享', new_issue_story_path(@phi.issue), :class => "button background-orange" 

                  - @phi.stories.each do |s|
                    %tr 
                      %td #{link_to s.title, issue_story_path(s.issue, s)}
                      %td #{s.comments_count}回应
                      %td 一天前
                      / %td #{s.last_updated_at.strftime("%Y-%m-%d %H:%M:%S")}
                      %td #{s.user.name}
            #tabcontent3.cell.hidden-tab
              %table.horizontal-border
                %tbody
                  - if @commented_stories.empty?
                    %p 
                      %b 我还没有回应任何分享！

                  - @commented_stories.each do |s|
                    %tr 
                      %td #{link_to s.title, issue_story_path(s.issue, s)}
                      %td #{s.comments_count}回应
                      %td 一天前
                      / %td #{s.last_updated_at.strftime("%Y-%m-%d %H:%M:%S")}
                      %td #{s.user.name}
          / - @stories.each do |story|
          /   .story.border-bottom.section-row{:style => "padding:15px;"} 
          /     .col
          /       .story-title
          /         %p
          /           #{link_to story.title, issue_story_path(story.issue, story)}
          /           %br
          /           %span{:style => "color:#999;font-size:80%;"} 
          /             #{story.impressionist_count(:filter=>:session_hash)}浏览 &middot; #{story.comments.size}评论 &middot; #{story.likers_count}有用 &middot; #{story.shares_count}鼓励 &middot; #{story.created_at.strftime("%Y-%m-%d %H:%M")} 
          /       .story-content   
          /         %p #{truncate(strip_tags(story.body), length: 140, omission: '.....')}    
  / .col.size3of4
  /   .margin-l20
  /     %h3.background-green{:style => "padding: 10px;line-height: 1.3;"} 胃病
  /     .tab-block.top-nav           
  /       .tabs        
  /         %ul.nav
  /           %li.active.active-item 
  /             = link_to '描述', '#'
  /           %li 
  /             = link_to '就医', '#'
  /           %li 
  /             = link_to '治疗', '#'            
  /           %li 
  /             = link_to '用药', '#'   
  /           %li 
  /             = link_to '护理', '#'     
  /           %li 
  /             = link_to '治愈', '#'
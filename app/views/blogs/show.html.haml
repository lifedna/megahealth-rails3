- @healthportal = "active"

/ Page
.site-center.center-page  
  .page-head{:style => "margin-bottom:30px;"}
    %h2 #{@phr.name}的健康
  .col.size1of4
    = render :partial => "shared/healthportal_sidebar", :locals => {:phr => @phr}  

  .col.sizefill
    .margin-l20
      %h2 健康日记
      %hr
      .col
        = link_to '返回', blogs_path(:phr => @phr), :class => "float-right"
      .col  
        %h3 #{@blog.title}
        %small 
          #{@blog.created_at.strftime("%Y-%m-%d %H:%M:%S")} #{@blog.category}
        %p #{raw @blog.body}
        %p #{@blog.tags}
        %hr
        %ul
          - @blog.comments.each do |comment|
            - if comment.persisted?
              %li
                #{comment.body}
        - if user_signed_in?        
          %p
            = form_tag comment_blog_path(@blog), :method => :get do
              %p
                = label_tag "你的回应"
              %p  
                = text_area_tag :comment, '', :size => "60x6" 
              %p
                = submit_tag '加上去'       